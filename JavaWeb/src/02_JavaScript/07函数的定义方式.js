// 定义一个无参函数
function func() {
    let a = 0;
    if (a) {
        console.log("true");
    } else {
        console.log("false");
    }
}

// 定义带有返回值的函数
function sum(num1, nums2) {
    let result = num1 + nums2;
    return result;
}