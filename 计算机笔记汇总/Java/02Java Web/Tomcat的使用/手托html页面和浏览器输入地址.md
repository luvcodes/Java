file://E:/book/index.html

使用的协议是file://协议。file协议表示告诉浏览器直接读取file:协议后面的路径，解析展示在浏览器上即可。

  

如果是在浏览器地址栏中输入访问地址格式如下: http://ip:port/工程名/资源名

所使用的是http协议。背后的原因完全不同。

  

客户端(浏览器)将请求发给服务器(Tomcat), 服务器收到请求之后，读取你要访问的资源文件，然后回传给客户端要的页面的内容 (回传index.html页面)